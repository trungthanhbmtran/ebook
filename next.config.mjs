/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'standalone',
  // Thêm các cấu hình Next.js khác nếu cần
  images: {
    qualities: [60, 75],
  },
};

export default nextConfig;
